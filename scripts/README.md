# Scripts

## `install-safe-solana-builder.sh`

Reinstalls the local `safe-solana-builder` bundle into Codex and/or Claude skill directories.

```bash
scripts/install-safe-solana-builder.sh
scripts/install-safe-solana-builder.sh --claude
scripts/install-safe-solana-builder.sh --both
```
