/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findSmokeStatePda } from "../pdas";
import { ARCADIA_VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const PING_DISCRIMINATOR = new Uint8Array([
  173, 0, 94, 236, 73, 133, 225, 153,
]);

export function getPingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PING_DISCRIMINATOR);
}

export type PingInstruction<
  TProgram extends string = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSmokeState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSmokeState extends string
        ? WritableAccount<TAccountSmokeState>
        : TAccountSmokeState,
      ...TRemainingAccounts,
    ]
  >;

export type PingInstructionData = {
  discriminator: ReadonlyUint8Array;
  message: string;
};

export type PingInstructionDataArgs = { message: string };

export function getPingInstructionDataEncoder(): Encoder<PingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["message", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: PING_DISCRIMINATOR }),
  );
}

export function getPingInstructionDataDecoder(): Decoder<PingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["message", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getPingInstructionDataCodec(): Codec<
  PingInstructionDataArgs,
  PingInstructionData
> {
  return combineCodec(
    getPingInstructionDataEncoder(),
    getPingInstructionDataDecoder(),
  );
}

export type PingAsyncInput<
  TAccountAuthority extends string = string,
  TAccountSmokeState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  smokeState?: Address<TAccountSmokeState>;
  message: PingInstructionDataArgs["message"];
};

export async function getPingInstructionAsync<
  TAccountAuthority extends string,
  TAccountSmokeState extends string,
  TProgramAddress extends Address = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
>(
  input: PingAsyncInput<TAccountAuthority, TAccountSmokeState>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PingInstruction<TProgramAddress, TAccountAuthority, TAccountSmokeState>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIA_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    smokeState: { value: input.smokeState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.smokeState.value) {
    accounts.smokeState.value = await findSmokeStatePda({
      authority: expectAddress(accounts.authority.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.smokeState),
    ],
    data: getPingInstructionDataEncoder().encode(
      args as PingInstructionDataArgs,
    ),
    programAddress,
  } as PingInstruction<TProgramAddress, TAccountAuthority, TAccountSmokeState>);
}

export type PingInput<
  TAccountAuthority extends string = string,
  TAccountSmokeState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  smokeState: Address<TAccountSmokeState>;
  message: PingInstructionDataArgs["message"];
};

export function getPingInstruction<
  TAccountAuthority extends string,
  TAccountSmokeState extends string,
  TProgramAddress extends Address = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
>(
  input: PingInput<TAccountAuthority, TAccountSmokeState>,
  config?: { programAddress?: TProgramAddress },
): PingInstruction<TProgramAddress, TAccountAuthority, TAccountSmokeState> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIA_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    smokeState: { value: input.smokeState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.smokeState),
    ],
    data: getPingInstructionDataEncoder().encode(
      args as PingInstructionDataArgs,
    ),
    programAddress,
  } as PingInstruction<TProgramAddress, TAccountAuthority, TAccountSmokeState>);
}

export type ParsedPingInstruction<
  TProgram extends string = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    smokeState: TAccountMetas[1];
  };
  data: PingInstructionData;
};

export function parsePingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), smokeState: getNextAccount() },
    data: getPingInstructionDataDecoder().decode(instruction.data),
  };
}
