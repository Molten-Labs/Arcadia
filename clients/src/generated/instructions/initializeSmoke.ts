/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findSmokeStatePda } from "../pdas";
import { ARCADIA_VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_SMOKE_DISCRIMINATOR = new Uint8Array([
  81, 152, 126, 223, 22, 188, 176, 110,
]);

export function getInitializeSmokeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_SMOKE_DISCRIMINATOR,
  );
}

export type InitializeSmokeInstruction<
  TProgram extends string = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSmokeState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSmokeState extends string
        ? WritableAccount<TAccountSmokeState>
        : TAccountSmokeState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeSmokeInstructionData = {
  discriminator: ReadonlyUint8Array;
  message: string;
};

export type InitializeSmokeInstructionDataArgs = { message: string };

export function getInitializeSmokeInstructionDataEncoder(): Encoder<InitializeSmokeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["message", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_SMOKE_DISCRIMINATOR }),
  );
}

export function getInitializeSmokeInstructionDataDecoder(): Decoder<InitializeSmokeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["message", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getInitializeSmokeInstructionDataCodec(): Codec<
  InitializeSmokeInstructionDataArgs,
  InitializeSmokeInstructionData
> {
  return combineCodec(
    getInitializeSmokeInstructionDataEncoder(),
    getInitializeSmokeInstructionDataDecoder(),
  );
}

export type InitializeSmokeAsyncInput<
  TAccountAuthority extends string = string,
  TAccountSmokeState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  smokeState?: Address<TAccountSmokeState>;
  systemProgram?: Address<TAccountSystemProgram>;
  message: InitializeSmokeInstructionDataArgs["message"];
};

export async function getInitializeSmokeInstructionAsync<
  TAccountAuthority extends string,
  TAccountSmokeState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
>(
  input: InitializeSmokeAsyncInput<
    TAccountAuthority,
    TAccountSmokeState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeSmokeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSmokeState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIA_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    smokeState: { value: input.smokeState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.smokeState.value) {
    accounts.smokeState.value = await findSmokeStatePda({
      authority: expectAddress(accounts.authority.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.smokeState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeSmokeInstructionDataEncoder().encode(
      args as InitializeSmokeInstructionDataArgs,
    ),
    programAddress,
  } as InitializeSmokeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSmokeState,
    TAccountSystemProgram
  >);
}

export type InitializeSmokeInput<
  TAccountAuthority extends string = string,
  TAccountSmokeState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  smokeState: Address<TAccountSmokeState>;
  systemProgram?: Address<TAccountSystemProgram>;
  message: InitializeSmokeInstructionDataArgs["message"];
};

export function getInitializeSmokeInstruction<
  TAccountAuthority extends string,
  TAccountSmokeState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
>(
  input: InitializeSmokeInput<
    TAccountAuthority,
    TAccountSmokeState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeSmokeInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSmokeState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIA_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    smokeState: { value: input.smokeState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.smokeState),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeSmokeInstructionDataEncoder().encode(
      args as InitializeSmokeInstructionDataArgs,
    ),
    programAddress,
  } as InitializeSmokeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSmokeState,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeSmokeInstruction<
  TProgram extends string = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    smokeState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeSmokeInstructionData;
};

export function parseInitializeSmokeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeSmokeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      smokeState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeSmokeInstructionDataDecoder().decode(instruction.data),
  };
}
