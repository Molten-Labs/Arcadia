/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { ARCADIA_VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SET_CAPACITY_DISCRIMINATOR = new Uint8Array([
  144, 85, 95, 65, 125, 139, 44, 27,
]);

export function getSetCapacityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_CAPACITY_DISCRIMINATOR,
  );
}

export type SetCapacityInstruction<
  TProgram extends string = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
  TAccountOracleAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProfile extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracleAuthority extends string
        ? ReadonlySignerAccount<TAccountOracleAuthority> &
            AccountSignerMeta<TAccountOracleAuthority>
        : TAccountOracleAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      ...TRemainingAccounts,
    ]
  >;

export type SetCapacityInstructionData = {
  discriminator: ReadonlyUint8Array;
  capUsd: bigint;
  scoreTier: number;
};

export type SetCapacityInstructionDataArgs = {
  capUsd: number | bigint;
  scoreTier: number;
};

export function getSetCapacityInstructionDataEncoder(): FixedSizeEncoder<SetCapacityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["capUsd", getU64Encoder()],
      ["scoreTier", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_CAPACITY_DISCRIMINATOR }),
  );
}

export function getSetCapacityInstructionDataDecoder(): FixedSizeDecoder<SetCapacityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["capUsd", getU64Decoder()],
    ["scoreTier", getU8Decoder()],
  ]);
}

export function getSetCapacityInstructionDataCodec(): FixedSizeCodec<
  SetCapacityInstructionDataArgs,
  SetCapacityInstructionData
> {
  return combineCodec(
    getSetCapacityInstructionDataEncoder(),
    getSetCapacityInstructionDataDecoder(),
  );
}

export type SetCapacityInput<
  TAccountOracleAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountProfile extends string = string,
> = {
  oracleAuthority: TransactionSigner<TAccountOracleAuthority>;
  config: Address<TAccountConfig>;
  profile: Address<TAccountProfile>;
  capUsd: SetCapacityInstructionDataArgs["capUsd"];
  scoreTier: SetCapacityInstructionDataArgs["scoreTier"];
};

export function getSetCapacityInstruction<
  TAccountOracleAuthority extends string,
  TAccountConfig extends string,
  TAccountProfile extends string,
  TProgramAddress extends Address = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
>(
  input: SetCapacityInput<
    TAccountOracleAuthority,
    TAccountConfig,
    TAccountProfile
  >,
  config?: { programAddress?: TProgramAddress },
): SetCapacityInstruction<
  TProgramAddress,
  TAccountOracleAuthority,
  TAccountConfig,
  TAccountProfile
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ARCADIA_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracleAuthority: {
      value: input.oracleAuthority ?? null,
      isWritable: false,
    },
    config: { value: input.config ?? null, isWritable: false },
    profile: { value: input.profile ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.oracleAuthority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.profile),
    ],
    data: getSetCapacityInstructionDataEncoder().encode(
      args as SetCapacityInstructionDataArgs,
    ),
    programAddress,
  } as SetCapacityInstruction<
    TProgramAddress,
    TAccountOracleAuthority,
    TAccountConfig,
    TAccountProfile
  >);
}

export type ParsedSetCapacityInstruction<
  TProgram extends string = typeof ARCADIA_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracleAuthority: TAccountMetas[0];
    config: TAccountMetas[1];
    profile: TAccountMetas[2];
  };
  data: SetCapacityInstructionData;
};

export function parseSetCapacityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetCapacityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      oracleAuthority: getNextAccount(),
      config: getNextAccount(),
      profile: getNextAccount(),
    },
    data: getSetCapacityInstructionDataDecoder().decode(instruction.data),
  };
}
