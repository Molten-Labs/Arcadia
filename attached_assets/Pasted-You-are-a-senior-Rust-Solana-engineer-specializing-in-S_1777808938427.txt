You are a senior Rust + Solana engineer specializing in SBF toolchain constraints and dependency graph isolation.

🎯 Objective

Refactor the given Rust Solana repository so that:

The on-chain program compiles with Solana SBF toolchain (Cargo 1.84 compatible)
The repository continues to support litesvm / solana 3.x testing tools
NO runtime behavior or business logic is removed or changed
NO features are lost
The project remains a single repository (monorepo allowed)
The fix must eliminate all edition2024 and incompatible Rust 2024 dependencies from SBF build
❌ Current Problem (must be solved)

The build currently fails because:

litesvm and solana 3.x dev stack pulls modern dependencies
These introduce:
toml_edit 0.25+spec
toml_datetime 1.1+spec
Rust edition2024
Solana SBF toolchain uses Cargo 1.84, which does NOT support edition2024

This causes build failure even though these are dev dependencies.

🧠 Required Solution Strategy

You MUST:

1. Separate build contexts inside same repository
Create a workspace structure
Keep everything in one repo (no loss of files unless strictly required for isolation)
2. Split into at least two crates:
A. SBF PROGRAM CRATE
Must compile with cargo build-sbf
Must NOT include:
litesvm
solana 3.x
any proc-macro chain that pulls edition2024 crates
Must use only SBF-compatible dependencies
B. TEST / DEV CRATE
Must include:
litesvm
solana 3.x
Must NOT affect SBF compilation
3. Dependency Isolation Rules
NEVER allow dev-dependencies to leak into program build graph
Ensure proc-macro dependencies do NOT propagate into SBF crate
Pin all Solana program dependencies to SBF-compatible versions
Ensure Cargo.lock reflects stable SBF-compatible graph
4. Constraints
Do NOT delete project logic
Do NOT rewrite business logic
Do NOT change architecture beyond necessary crate separation
Keep repo functional as a workspace
Maintain ability to run tests with litesvm
📦 Required Output

The agent must output:

Final directory structure
Updated Cargo.toml (workspace root)
Program crate Cargo.toml
Test crate Cargo.toml
Explanation of what was moved and why
Exact command to build SBF successfully
🚨 Success Criteria
cargo build-sbf succeeds with no edition2024 errors
cargo test works with litesvm
No dependency graph contamination between program and tests
Repo remains a single unified workspace
⚠️ Important

If any dependency (direct or transitive) introduces:

toml_edit 0.25+
proc-macro-crate 3.x
edition2024

You must isolate or downgrade it so it NEVER enters the SBF crate dependency graph.